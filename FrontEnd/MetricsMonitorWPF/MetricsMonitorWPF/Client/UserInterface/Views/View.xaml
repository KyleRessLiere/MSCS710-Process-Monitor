<UserControl x:Class="MetricsMonitorWPF.Client.UserInterface.Views.View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MetricsMonitorWPF.Client.UserInterface.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <TextBlock>How are we doing?</TextBlock>
            <local:RowOfButtons DataContext="{Binding RatingScale}"/>
            <TextBlock>Pick your favorite person:</TextBlock>
            <local:RowOfButtons DataContext="{Binding People}"/>
        </StackPanel>
    </Grid>
</UserControl>
